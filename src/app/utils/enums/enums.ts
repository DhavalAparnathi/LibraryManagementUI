export enum UserRole {
  ADMIN = 1,
  HOD = 2,
  TEACHER = 3,
  ASSISTANT_TEACHER = 4,
  STUDENT = 5,
}

export enum ConfirmationStatus {
  PENDING = 1,
  APPROVED = 2,
  REJECTED = 3,
  CANCELLED = 4,
}

export enum AllRoleType {
  ADMIN = 'Admin',
  HOD = 'HOD',
  TEACHER = 'Teacher',
  ASSISTANT_TEACHER = 'Ass. Teacher',
  STUDENT = 'Student',
}

export enum AllStatusType {
  PENDING = 'Pending',
  APPROVED = 'Approved',
  REJECTED = 'Rejected',
}
